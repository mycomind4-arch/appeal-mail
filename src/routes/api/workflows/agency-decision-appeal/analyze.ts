import {createFileRoute} from "@tanstack/react-router";import {requireAuthenticatedUser,getSupabaseServer} from "@/platform/supabase";import {uploadDocument} from "@/platform/mailmypdf";
export const Route=createFileRoute("/api/workflows/agency-decision-appeal/analyze")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const form=await request.formData();const file=form.get("document");if(!(file instanceof File)||file.size===0)return Response.json({error:"An agency decision is required."},{status:400});if(file.size>20*1024*1024)return Response.json({error:"Source documents must be 20 MB or smaller."},{status:413});if(!["application/pdf","image/png","image/jpeg"].includes(file.type))return Response.json({error:"Accepted source types are PDF, PNG, and JPEG."},{status:415});const token=process.env.MAILMYPDF_CONTROL_PLANE_TOKEN;if(!token)throw new Error("MailMyPDF control-plane token is not configured.");const base=process.env.MAILMYPDF_CONTROL_PLANE_URL||"https://mailmypdf.com";const c=await fetch(`${base}/api/control-plane/ai`,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({verticalSlug:"appeal-mail",workflowSlug:"agency-decision-appeal",task:"analysis"})});const cfg=await c.json().catch(()=>null) as any;if(!c.ok)throw new Error(cfg?.error||"Control plane error.");if(cfg.provider!=="gemini")throw new Error("Agency Decision Appeal is currently configured for Gemini.");const prompt=cfg.promptOverride||["You are an authority-first analyst for an agency decision appeal.","Return strict JSON only. Extract only source-supported information.","Never invent agency deadlines, forms, recipients, filing destinations, hearing rights, exhaustion rules, facts, or outcomes.",'{"summary":"","issuer":"","jurisdiction":"","referenceNumber":"","decisionDate":"","deadline":"","deadlineStatus":"extracted|verified|unverified","appealInstructions":"","findings":[],"disputedFacts":[],"evidenceGaps":[],"contradictions":[],"citedAuthority":[],"authoritySources":[],"timeline":[],"uncertainties":[],"confidence":"high|medium|low"}'].join("\n");const bytes=Buffer.from(await file.arrayBuffer()).toString("base64");const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(cfg.model)}:generateContent?key=${encodeURIComponent(cfg.apiKey)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts:[{inlineData:{mimeType:file.type,data:bytes}},{text:prompt}]}],generationConfig:{responseMimeType:"application/json",temperature:.1}})});const b=await r.json().catch(()=>null) as any;if(!r.ok)throw new Error(b?.error?.message||"Gemini analysis failed.");const text=b?.candidates?.[0]?.content?.parts?.map((p:any)=>p.text||"").join("").trim();if(!text)throw new Error("Gemini returned no analysis.");const a=JSON.parse(text);const doc=await uploadDocument(file);const now=new Date().toISOString();const decision={id:crypto.randomUUID(),type:"agency_decision",documentId:doc.id,documentFilename:file.name,...a,extractedAt:now};const appealId=crypto.randomUUID();const supabase=await getSupabaseServer();const {error}=await supabase.from("appeals").insert({id:appealId,user_id:user.id,workflow_id:"agency-decision-appeal",status:"in_progress",decision,grounds:[],evidence:[{id:crypto.randomUUID(),type:"document",label:file.name,documentId:doc.id,uploadedAt:now}],arguments:[],draft:"",review:null,packet:null,proof:null,timeline:decision.timeline||[],version:1,created_at:now,updated_at:now});if(error)throw new Error(error.message);return Response.json({ok:true,appealId,documentId:doc.id,analysis:a})}catch(e){const m=e instanceof Error?e.message:"Unable to analyze agency decision.";return Response.json({error:m},{status:/authentication|required|token/i.test(m)?401:502})}}}}});
