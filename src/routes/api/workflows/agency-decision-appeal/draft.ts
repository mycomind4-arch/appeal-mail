import {createFileRoute} from "@tanstack/react-router";import {requireAuthenticatedUser,getSupabaseServer} from "@/platform/supabase";export const Route=createFileRoute("/api/workflows/agency-decision-appeal/draft")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const{appealId}=await request.json() as{appealId?:string};if(!appealId)return Response.json({error:"Appeal id is required."},{status:400});const supabase=await getSupabaseServer();const{data:a,error}=await supabase.from("appeals").select("*").eq("id",appealId).single();if(error||!a)return Response.json({error:"Appeal case not found."},{status:404});if(a.user_id!==user.id)return Response.json({error:"You do not own this appeal."},{status:403});const token=process.env.MAILMYPDF_CONTROL_PLANE_TOKEN;if(!token)throw new Error("MailMyPDF control-plane token is not configured.");const base=process.env.MAILMYPDF_CONTROL_PLANE_URL||"https://mailmypdf.com";const c=await fetch(`${base}/api/control-plane/ai`,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({verticalSlug:"appeal-mail",workflowSlug:"agency-decision-appeal",task:"draft"})});const cfg=await c.json().catch(()=>null) as any;if(!c.ok)throw new Error(cfg?.error||"Control plane error.");if(cfg.provider!=="gemini")throw new Error("Agency Decision Appeal is currently configured for Gemini.");const prompt=cfg.promptOverride||["Draft a professional agency decision appeal using only the supplied analysis.","Never invent facts, deadlines, forms, filing destinations, recipients, hearing rights, exhaustion requirements, legal authorities, or outcomes.","Clearly distinguish supported facts, disputed findings, evidence gaps, and unresolved procedure.",JSON.stringify(a.decision)].join("\n\n");const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(cfg.model)}:generateContent?key=${encodeURIComponent(cfg.apiKey)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts:[{text:prompt}]}],generationConfig:{temperature:.2}})});const b=await r.json().catch(()=>null) as any;if(!r.ok)throw new Error(b?.error?.message||"Gemini drafting failed.");const draft=b?.candidates?.[0]?.content?.parts?.map((p:any)=>p.text||"").join("").trim();if(!draft)throw new Error("Gemini returned no draft.");await supabase.from("appeals").update({draft,status:"in_progress",updated_at:new Date().toISOString()}).eq("id",appealId).eq("user_id",user.id);return Response.json({ok:true,appealId,draft,provider:"gemini",model:cfg.model})}catch(e){const m=e instanceof Error?e.message:"Unable to draft agency appeal.";return Response.json({error:m},{status:/authentication|required|token/i.test(m)?401:502})}}}}});