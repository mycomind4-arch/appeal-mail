import { createFileRoute } from "@tanstack/react-router";import { requireAuthenticatedUser,getSupabaseServer } from "@/platform/supabase";import { BENEFITS_DENIAL_APPEAL_PRICING } from "@/domain/benefits-denial-appeal-gold";
export const Route=createFileRoute("/api/workflows/benefits-denial-appeal/approve")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const input=await request.json() as {appealId?:string;recipient?:{name?:string;address1?:string;address2?:string;city?:string;state?:string;zip?:string};mailingMethod?:"standard"|"certified"|"registered";responseSheets?:number;supportingSheets?:number};if(!input.appealId)return Response.json({error:"Appeal id is required."},{status:400});const r=input.recipient;if(!r?.name||!r.address1||!r.city||!r.state||!r.zip)return Response.json({error:"A complete mailing recipient is required."},{status:400});if(!input.mailingMethod)return Response.json({error:"Mailing method is required."},{status:400});const s=await getSupabaseServer();const {data:a,error}=await s.from("appeals").select("*").eq("id",input.appealId).single();if(error||!a)return Response.json({error:"Appeal case not found."},{status:404});if(a.user_id!==user.id)return Response.json({error:"You do not own this appeal."},{status:403});if(a.workflow_id!=="benefits-denial-appeal")return Response.json({error:"Workflow mismatch."},{status:409});if(!a.draft?.trim())return Response.json({error:"Draft is required before approval."},{status:409});const reviewData=a.review?.independentValidation;const blocking=Array.isArray(reviewData?.blockingIssues)?reviewData.blockingIssues:[];if(blocking.length)return Response.json({error:"Independent validation found blocking issues.",validation:reviewData},{status:409});const d=a.decision||{};const unresolved=Array.isArray(d.authoritySources)?d.authoritySources.filter((x:any)=>["unverified","conflicting"].includes(x.verificationState)):[];const gaps=Array.isArray(d.evidenceGaps)?d.evidenceGaps:[];const contradictions=Array.isArray(d.contradictions)?d.contradictions:[];const review={...(a.review||{}),score:Math.max(0,100-unresolved.length*8-gaps.length*4-contradictions.length*4),unresolvedAuthority:unresolved,evidenceGaps:gaps,contradictions,humanApprovalRequired:true,approvedAt:new Date().toISOString()};if(review.score<70)return Response.json({error:"Benefits appeal is not ready for approval.",review},{status:409});const packet={id:crypto.randomUUID(),recipientName:r.name,recipientAddress1:r.address1,recipientAddress2:r.address2,recipientCity:r.city,recipientState:r.state,recipientZip:r.zip,mailingMethod:input.mailingMethod,workflowId:"benefits-denial-appeal",finalDocumentType:"response-pdf",responseSheets:Math.max(1,Number(input.responseSheets||3)),supportingSheets:Math.max(0,Number(input.supportingSheets||0)),pricing:{...BENEFITS_DENIAL_APPEAL_PRICING}};const {error:updateError}=await s.from("appeals").update({status:"ready",review,packet,version:(a.version||1)+1,updated_at:new Date().toISOString()}).eq("id",input.appealId).eq("user_id",user.id).eq("version",a.version||1);if(updateError)throw new Error(updateError.message);return Response.json({ok:true,appealId:input.appealId,status:"ready",review,packet});}catch(error){const m=error instanceof Error?error.message:"Unable to approve benefits appeal.";return Response.json({error:m},{status:/authentication|required/i.test(m)?401:502});}}}}});
