import { createFileRoute } from "@tanstack/react-router";import { requireAuthenticatedUser,getSupabaseServer } from "@/platform/supabase";
export const Route=createFileRoute("/api/workflows/benefits-denial-appeal/validate")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const {appealId}=await request.json() as {appealId?:string};if(!appealId)return Response.json({error:"Appeal id is required."},{status:400});const s=await getSupabaseServer();const {data:a,error}=await s.from("appeals").select("*").eq("id",appealId).single();if(error||!a)return Response.json({error:"Appeal case not found."},{status:404});if(a.user_id!==user.id)return Response.json({error:"You do not own this appeal."},{status:403});if(!a.draft?.trim())return Response.json({error:"Draft is required before validation."},{status:409});const token=process.env.MAILMYPDF_CONTROL_PLANE_TOKEN;const base=process.env.MAILMYPDF_CONTROL_PLANE_URL||"https://mailmypdf.com";if(!token)throw new Error("MailMyPDF control-plane token is not configured.");const c=await fetch(`${base.replace(/\/$/,"")}/api/control-plane/ai`,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({verticalSlug:"appeal-mail",workflowSlug:"benefits-denial-appeal",task:"validation"})});const cfg=await c.json().catch(()=>null) as any;if(!c.ok)throw new Error(cfg?.error||`Control plane error (${c.status}).`);if(cfg.provider!=="gemini")throw new Error("Benefits Denial Appeal is currently configured for Gemini.");const prompt=cfg.promptOverride||`Independently validate this government benefits appeal draft. Return strict JSON. Identify invented facts, unsupported procedural claims, deadline claims, recipient/form assumptions, contradictions with the denial, missing requested relief, and unclear evidence references. Never add legal or benefit conclusions. Use: {"score":0,"blockingIssues":[],"warnings":[],"unsupportedClaims":[],"evidenceIssues":[],"authorityIssues":[],"readyForHumanReview":false}`+`\nDecision: ${JSON.stringify(a.decision)}\nDraft: ${a.draft}`;const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(cfg.model)}:generateContent?key=${encodeURIComponent(cfg.apiKey)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts:[{text:prompt}]}],generationConfig:{responseMimeType:"application/json",temperature:0.1}})});const b=await r.json().catch(()=>null) as any;if(!r.ok)throw new Error(b?.error?.message||`Gemini validation failed (${r.status}).`);const text=b?.candidates?.[0]?.content?.parts?.map((p:any)=>p.text||"").join("").trim();if(!text)throw new Error("Gemini returned no validation.");const validation=JSON.parse(text);await s.from("appeals").update({review:{...(a.review||{}),independentValidation:validation},updated_at:new Date().toISOString()}).eq("id",appealId).eq("user_id",user.id);return Response.json({ok:true,appealId,validation,provider:"gemini",model:cfg.model});}catch(error){const m=error instanceof Error?error.message:"Unable to validate benefits appeal.";return Response.json({error:m},{status:/authentication|required|token/i.test(m)?401:502});}}}}});
