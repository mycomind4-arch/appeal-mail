import { createFileRoute } from "@tanstack/react-router";import { requireAuthenticatedUser,getSupabaseServer } from "@/platform/supabase";import { runReadinessReview } from "@/domain/review";import { assemblePacket } from "@/domain/packet";
export const Route=createFileRoute("/api/workflows/long-term-disability-denial/approve")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const i=await request.json() as any;if(!i.appealId)return Response.json({error:"Appeal id is required."},{status:400});const r=i.recipient;if(!r?.name||!r.address1||!r.city||!r.state||!r.zip)return Response.json({error:"A complete mailing recipient is required."},{status:400});if(!i.mailingMethod)return Response.json({error:"Mailing method is required."},{status:400});const s=await getSupabaseServer(),{data:a,error}=await s.from("appeals").select("*").eq("id",i.appealId).single();if(error||!a)return Response.json({error:"Appeal case not found."},{status:404});if(a.user_id!==user.id)return Response.json({error:"You do not own this appeal case."},{status:403});if(a.workflow_id!=="long-term-disability-denial")return Response.json({error:"Appeal workflow mismatch."},{status:409});if(!a.draft?.trim())return Response.json({error:"The appeal draft must be created before approval."},{status:409});const evidence=Array.isArray(a.evidence)?a.evidence:[],grounds=Array.isArray(a.grounds)?a.grounds:[];const review=runReadinessReview({decision:a.decision,grounds,evidence,draft:a.draft,recipient:r,exhibitCount:evidence.length,hasSignature:/sincerely[,\s]*$/im.test(a.draft)||/\[your name\]/i.test(a.draft)});if(review.score<80||review.issuesRequiringAttention>2||review.checks.some((c:any)=>c.status==="fail"))return Response.json({error:"Appeal is not ready for approval.",review},{status:409});const packet=assemblePacket({appealId:i.appealId,finalLetter:a.draft,evidence,recipient:r,mailingMethod:i.mailingMethod});const ver=a.version??1;const {error:u}=await s.from("appeals").update({status:"ready",review,packet,version:ver+1,updated_at:new Date().toISOString()}).eq("id",i.appealId).eq("user_id",user.id).eq("version",ver);if(u)throw new Error(`Unable to approve appeal: ${u.message}`);return Response.json({ok:true,appealId:i.appealId,status:"ready",review,packet});}catch(error){const message=error instanceof Error?error.message:"Unable to approve long-term disability appeal.";return Response.json({error:message},{status:/authentication|required|token/i.test(message)?401:502});}}}}});
