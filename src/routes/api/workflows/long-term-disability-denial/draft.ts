import { createFileRoute } from "@tanstack/react-router";import { requireAuthenticatedUser,getSupabaseServer } from "@/platform/supabase";import { getWorkflow } from "@/domain/workflows";
async function cfg(task:"draft"|"validation"){const base=process.env.MAILMYPDF_CONTROL_PLANE_URL||"https://mailmypdf.com",token=process.env.MAILMYPDF_CONTROL_PLANE_TOKEN;if(!token)throw new Error("MailMyPDF control-plane token is not configured.");const r=await fetch(`${base.replace(/\/$/,"")}/api/control-plane/ai`,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({verticalSlug:"appeal-mail",workflowSlug:"long-term-disability-denial",task})});const p=await r.json().catch(()=>null) as any;if(!r.ok||p?.provider!=="gemini"||!p.apiKey||!p.model)throw new Error("Gemini configuration is unavailable for this workflow.");return p;}
async function call(c:any,p:string){const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(c.model)}:generateContent?key=${encodeURIComponent(c.apiKey)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts:[{text:c.promptOverride||p}]}],generationConfig:{temperature:.2}})});const b=await r.json().catch(()=>null) as any;if(!r.ok)throw new Error(b?.error?.message||`Gemini request failed (${r.status}).`);const t=b?.candidates?.[0]?.content?.parts?.map((x:any)=>x.text||"").join("").trim();if(!t)throw new Error("Gemini returned no response.");return t;}
export const Route=createFileRoute("/api/workflows/long-term-disability-denial/draft")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const input=await request.json() as any;if(!input.appealId)return Response.json({error:"Appeal id is required."},{status:400});const s=await getSupabaseServer(),{data:a,error}=await s.from("appeals").select("*").eq("id",input.appealId).single();if(error||!a)return Response.json({error:"Appeal case not found."},{status:404});if(a.user_id!==user.id)return Response.json({error:"You do not own this appeal case."},{status:403});if(a.workflow_id!=="long-term-disability-denial")return Response.json({error:"Appeal workflow mismatch."},{status:409});const wf=getWorkflow("long-term-disability-denial"),d=await cfg("draft"),v=await cfg("validation"),analysis=input.analysis||a.decision;const draft=input.draftOverride?.trim()||await call(d,[`Create a response for ${wf.title}.`,wf.workflowPrompt,`Focus on: ${wf.focusAreas.join(", ")}.`,"Use only supplied facts. Do not invent diagnoses, functional limits, policy definitions, occupation standards, deadlines, legal authority, or outcomes.","Write a professional long-term disability appeal a human can review and edit.",`CASE ANALYSIS:\n${JSON.stringify(analysis)}`].join("\n\n"));const validation=await call(v,[`Audit this ${wf.title} draft.`,"Return strict JSON with valid, issues, unsupportedClaims, missingEvidence, suggestions.","Flag invented policy language, unsupported medical or functional assertions, unsupported occupation-standard claims, missing evidence, contradictions, deadline issues, and uncertainty.",`CASE ANALYSIS:\n${JSON.stringify(analysis)}`,`DRAFT:\n${draft}`].join("\n\n"));const persisted=`${draft}\n\nSincerely,\n[Your Name]`,ver=a.version??1;const {error:u}=await s.from("appeals").update({draft:persisted,status:"in_progress",version:ver+1,updated_at:new Date().toISOString()}).eq("id",a.id).eq("user_id",user.id).eq("version",ver);if(u)throw new Error(`Unable to persist draft: ${u.message}`);return Response.json({ok:true,appealId:a.id,draft:persisted,validation,provider:"gemini",draftModel:d.model,validationModel:v.model});}catch(error){const message=error instanceof Error?error.message:"Unable to create long-term disability appeal response.";return Response.json({error:message},{status:/authentication|required|token/i.test(message)?401:502});}}}}});
