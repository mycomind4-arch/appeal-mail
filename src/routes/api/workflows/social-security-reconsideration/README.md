# Social Security Reconsideration API

Gold-standard workflow boundary: `analyze` → `draft` → `validate` → `approve` → `checkout` → shared Stripe fulfillment/proof.

AI provider is resolved through the MailMyPDF control plane and currently requires Gemini.
