import { createFileRoute } from "@tanstack/react-router"; import { requireAuthenticatedUser,getSupabaseServer } from "@/platform/supabase"; import { uploadDocument } from "@/platform/mailmypdf";
export const Route=createFileRoute("/api/workflows/va-claim-appeal/analyze")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const form=await request.formData();const file=form.get("document");if(!(file instanceof File))return Response.json({error:"A VA decision letter is required."},{status:400});if(file.size===0)return Response.json({error:"The source document is empty."},{status:400});if(file.size>20*1024*1024)return Response.json({error:"Source documents must be 20 MB or smaller."},{status:413});if(!["application/pdf","image/png","image/jpeg"].includes(file.type))return Response.json({error:"Accepted source types are PDF, PNG, and JPEG."},{status:415});const token=process.env.MAILMYPDF_CONTROL_PLANE_TOKEN;const base=process.env.MAILMYPDF_CONTROL_PLANE_URL||"https://mailmypdf.com";if(!token)throw new Error("MailMyPDF control-plane token is not configured.");const c=await fetch(`${base.replace(/\/$/,"")}/api/control-plane/ai`,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({verticalSlug:"appeal-mail",workflowSlug:"va-claim-appeal",task:"analysis"})});const cfg=await c.json().catch(()=>null) as any;if(!c.ok)throw new Error(cfg?.error||`Control plane error (${c.status}).`);if(cfg.provider!=="gemini")throw new Error("VA Claim Appeal is currently configured for Gemini.");const prompt=cfg.promptOverride||["You are the authority-first analyst for a VA claim decision review workflow.","Use only the source document and supplied VA authority. Return strict JSON only.","Never invent service history, medical facts, ratings, effective dates, claim issues, deadlines, forms, recipients, eligibility, evidence, or outcomes.","Distinguish Supplemental Claim, Higher-Level Review, and Board Appeal. A Higher-Level Review cannot consider new evidence. Supplemental Claims require new and relevant evidence. Board evidence rules depend on lane.",'{"summary":"","decisionDate":"","claimReference":"","benefitType":"","issues":[],"reasonsAndBases":[],"evidenceConsidered":[],"disputedFacts":[],"medicalFactsMentioned":[],"serviceFactsMentioned":[],"potentialErrors":[],"evidenceGaps":[],"contradictions":[],"recommendedReviewLane":"supplemental|higher-level|board|unresolved","laneReason":"","deadline":"","deadlineStatus":"extracted|verified|unverified","formsMentioned":[],"filingInstructions":"","authoritySources":[],"uncertainties":[],"confidence":"high|medium|low"}'].join("\n");const bytes=Buffer.from(await file.arrayBuffer()).toString("base64");const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(cfg.model)}:generateContent?key=${encodeURIComponent(cfg.apiKey)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts:[{inlineData:{mimeType:file.type,data:bytes}},{text:prompt}]}],generationConfig:{responseMimeType:"application/json",temperature:0.1}})});const b=await r.json().catch(()=>null) as any;if(!r.ok)throw new Error(b?.error?.message||`Gemini analysis failed (${r.status}).`);const text=b?.candidates?.[0]?.content?.parts?.map((p:{text?:string})=>p.text||"").join("").trim();if(!text)throw new Error("Gemini returned no analysis.");const analysis=JSON.parse(text) as Record<string,unknown>;const sourceDocument=await uploadDocument(file);const appealId=crypto.randomUUID();const now=new Date().toISOString();const decision={id:crypto.randomUUID(),type:"va_claim_decision",documentId:sourceDocument.id,documentFilename:file.name,decisionDate:analysis.decisionDate||"",claimReference:analysis.claimReference||"",benefitType:analysis.benefitType||"",issues:Array.isArray(analysis.issues)?analysis.issues:[],reasonsAndBases:Array.isArray(analysis.reasonsAndBases)?analysis.reasonsAndBases:[],evidenceConsidered:Array.isArray(analysis.evidenceConsidered)?analysis.evidenceConsidered:[],disputedFacts:Array.isArray(analysis.disputedFacts)?analysis.disputedFacts:[],medicalFactsMentioned:Array.isArray(analysis.medicalFactsMentioned)?analysis.medicalFactsMentioned:[],serviceFactsMentioned:Array.isArray(analysis.serviceFactsMentioned)?analysis.serviceFactsMentioned:[],potentialErrors:Array.isArray(analysis.potentialErrors)?analysis.potentialErrors:[],evidenceGaps:Array.isArray(analysis.evidenceGaps)?analysis.evidenceGaps:[],contradictions:Array.isArray(analysis.contradictions)?analysis.contradictions:[],recommendedReviewLane:analysis.recommendedReviewLane||"unresolved",laneReason:analysis.laneReason||"",deadline:analysis.deadline?{date:analysis.deadline,status:analysis.deadlineStatus||"unverified",source:"extracted"}:undefined,formsMentioned:Array.isArray(analysis.formsMentioned)?analysis.formsMentioned:[],filingInstructions:analysis.filingInstructions||"",authoritySources:Array.isArray(analysis.authoritySources)?analysis.authoritySources:[],uncertainties:Array.isArray(analysis.uncertainties)?analysis.uncertainties:[],extractedAt:now};const supabase=await getSupabaseServer();const {error}=await supabase.from("appeals").insert({id:appealId,user_id:user.id,workflow_id:"va-claim-appeal",status:"in_progress",decision,grounds:[],evidence:[{id:crypto.randomUUID(),type:"document",label:file.name,documentId:sourceDocument.id,uploadedAt:now}],arguments:[],draft:"",review:null,packet:null,proof:null,timeline:[],version:1,created_at:now,updated_at:now});if(error)throw new Error(`Unable to save appeal: ${error.message}`);return Response.json({ok:true,appealId,documentId:sourceDocument.id,analysis});}catch(error){const message=error instanceof Error?error.message:"Unable to analyze VA decision.";return Response.json({error:message},{status:/authentication|required|token/i.test(message)?401:502});}}}}});
