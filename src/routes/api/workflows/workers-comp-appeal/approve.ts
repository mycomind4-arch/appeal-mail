import { createFileRoute } from "@tanstack/react-router";
import { requireAuthenticatedUser,getSupabaseServer } from "@/platform/supabase";
import { WORKERS_COMP_APPEAL_PRICING } from "@/domain/workers-comp-appeal-gold";
export const Route=createFileRoute("/api/workflows/workers-comp-appeal/approve")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const input=await request.json() as {appealId?:string;recipient?:{name?:string;address1?:string;address2?:string;city?:string;state?:string;zip?:string};mailingMethod?:"standard"|"certified"|"registered";responseSheets?:number;supportingSheets?:number};if(!input.appealId)return Response.json({error:"Appeal id is required."},{status:400});const r=input.recipient;if(!r?.name||!r.address1||!r.city||!r.state||!r.zip)return Response.json({error:"A complete mailing recipient is required."},{status:400});if(!input.mailingMethod)return Response.json({error:"Mailing method is required."},{status:400});const supabase=await getSupabaseServer();const {data:a,error}=await supabase.from("appeals").select("*").eq("id",input.appealId).single();if(error||!a)return Response.json({error:"Appeal case not found."},{status:404});if(a.user_id!==user.id)return Response.json({error:"You do not own this appeal."},{status:403});if(a.workflow_id!=="workers-comp-appeal")return Response.json({error:"Workflow mismatch."},{status:409});if(!a.draft?.trim())return Response.json({error:"Draft is required before approval."},{status:409});const review=a.review||{};if(review.pass!==true||Number(review.score||0)<70)return Response.json({error:"Workers' compensation appeal has not passed independent validation."},{status:409});const d=a.decision||{};const unresolved=Array.isArray(d.authoritySources)?d.authoritySources.filter((s:any)=>["unverified","conflicting"].includes(s.verificationState)):[];if(unresolved.length>0)return Response.json({error:"Unresolved authority sources must be resolved before approval.",unresolvedAuthority:unresolved},{status:409});const packet={id:crypto.randomUUID(),recipientName:r.name,recipientAddress1:r.address1,recipientAddress2:r.address2,recipientCity:r.city,recipientState:r.state,recipientZip:r.zip,mailingMethod:input.mailingMethod,workflowId:"workers-comp-appeal",finalDocumentType:"response-pdf",responseSheets:Math.max(1,Number(input.responseSheets||4)),supportingSheets:Math.max(0,Number(input.supportingSheets||0)),pricing:{...WORKERS_COMP_APPEAL_PRICING}};const finalReview={...review,humanApprovalRequired:true,approvedAt:new Date().toISOString(),unresolvedAuthority:unresolved};const {error:updateError}=await supabase.from("appeals").update({status:"ready",review:finalReview,packet,version:(a.version||1)+1,updated_at:new Date().toISOString()}).eq("id",input.appealId).eq("user_id",user.id).eq("version",a.version||1);if(updateError)throw new Error(updateError.message);return Response.json({ok:true,appealId:input.appealId,status:"ready",review:finalReview,packet});}catch(error){const message=error instanceof Error?error.message:"Unable to approve workers' compensation appeal.";return Response.json({error:message},{status:/authentication|required|token/i.test(message)?401:502});}}}}});
