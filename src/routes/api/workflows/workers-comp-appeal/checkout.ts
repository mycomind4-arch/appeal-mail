import { createFileRoute } from "@tanstack/react-router";
import { requireAuthenticatedUser,getSupabaseServer } from "@/platform/supabase";
export const Route=createFileRoute("/api/workflows/workers-comp-appeal/checkout")({server:{handlers:{POST:async({request})=>{try{const user=await requireAuthenticatedUser(request);const {appealId}=await request.json() as {appealId?:string};if(!appealId)return Response.json({error:"Appeal id is required."},{status:400});const supabase=await getSupabaseServer();const {data:a,error}=await supabase.from("appeals").select("*").eq("id",appealId).single();if(error||!a)return Response.json({error:"Appeal case not found."},{status:404});if(a.user_id!==user.id)return Response.json({error:"You do not own this appeal."},{status:403});if(a.status!=="ready"||!a.packet)return Response.json({error:"Appeal is not approved and ready for payment."},{status:409});const p=a.packet.pricing as any;const method=a.packet.mailingMethod as "standard"|"certified"|"registered";const responseSheets=Math.max(1,Number(a.packet.responseSheets||4));const supportingSheets=Math.max(0,Number(a.packet.supportingSheets||0));const included=Number(p.includedResponsePages||4);const preparation=Number(p.preparationFee||34.99);const responseExtra=Math.max(0,responseSheets-included)*Number(p.responsePagePrice||0.45);const supportCost=supportingSheets*Number(p.supportingPagePrice||0.25);const mail=method==="certified"?Number(p.certifiedMail||12.49):method==="registered"?Number(p.registeredMail||29.99):Number(p.standardMail||5.49);const flat=responseSheets+supportingSheets>7?Number(p.flatEnvelopeFee||2.5):0;const total=Math.round((preparation+responseExtra+supportCost+mail+flat)*100)/100;const {default:Stripe}=await import("stripe");const secret=process.env.STRIPE_SECRET_KEY;if(!secret)return Response.json({error:"Stripe is not configured."},{status:503});const stripe=new Stripe(secret,{apiVersion:"2024-06-20" as Stripe.LatestApiVersion});const appUrl=process.env.APP_URL||"https://appeal-mail.pages.dev";const session=await stripe.checkout.sessions.create({mode:"payment",payment_method_types:["card"],line_items:[{price_data:{currency:"usd",product_data:{name:`Workers' Compensation Appeal — ${method} mailing`},unit_amount:Math.round(total*100)},quantity:1}],metadata:{appeal_id:a.id,workflow_id:"workers-comp-appeal",mailing_method:method,recipient_name:a.packet.recipientName,owner_user_id:user.id,response_sheets:String(responseSheets),supporting_sheets:String(supportingSheets),total:String(total)},success_url:`${appUrl}/workflows/workers-comp-appeal?checkout=success&session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${appUrl}/workflows/workers-comp-appeal?checkout=cancelled`});return Response.json({ok:true,sessionId:session.id,url:session.url,pricing:{preparation,responseExtra,supportCost,mail,flat,total}});}catch(error){const message=error instanceof Error?error.message:"Unable to create workers' compensation checkout.";return Response.json({error:message},{status:/authentication|required|token/i.test(message)?401:502});}}}}});
