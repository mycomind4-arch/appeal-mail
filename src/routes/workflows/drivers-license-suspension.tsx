import { createFileRoute } from "@tanstack/react-router";
import { DriversLicenseSuspensionWorkspace } from "@/components/workflow/drivers-license-suspension-workspace";
import { getWorkflow } from "@/domain/workflows";
export const Route=createFileRoute("/workflows/drivers-license-suspension")({head:()=>({meta:[{title:`${getWorkflow("drivers-license-suspension").title} — Appeal Mail`},{name:"description",content:getWorkflow("drivers-license-suspension").description}, { property: "og:title", content: `${getWorkflow("drivers-license-suspension").title} — Appeal Mail` }, { property: "og:description", content: getWorkflow("drivers-license-suspension").description }, { name: "twitter:card", content: "summary" }, { name: "twitter:title", content: `${getWorkflow("drivers-license-suspension").title} — Appeal Mail` }, { name: "twitter:description", content: getWorkflow("drivers-license-suspension").description }], links:[{rel:"canonical",href:"/workflows/drivers-license-suspension"}]}),component:DriversLicenseSuspensionWorkspace});
