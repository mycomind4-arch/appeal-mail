import { createFileRoute } from "@tanstack/react-router";
import { LicenseRevocationAppealWorkspace } from "@/components/workflow/license-revocation-appeal-workspace";
import { getWorkflow } from "@/domain/workflows";
export const Route = createFileRoute("/workflows/license-revocation-appeal")({ head: () => ({ meta: [{ title: `${getWorkflow("license-revocation-appeal").title} — Appeal Mail` }, { name: "description", content: getWorkflow("license-revocation-appeal").description }, { property: "og:title", content: `${getWorkflow("license-revocation-appeal").title} — Appeal Mail` }, { property: "og:description", content: getWorkflow("license-revocation-appeal").description }, { name: "twitter:card", content: "summary" }, { name: "twitter:title", content: `${getWorkflow("license-revocation-appeal").title} — Appeal Mail` }, { name: "twitter:description", content: getWorkflow("license-revocation-appeal").description }], links: [{ rel: "canonical", href: "/workflows/license-revocation-appeal" }] }), component: LicenseRevocationAppealWorkspace });
