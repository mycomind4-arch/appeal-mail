import { createFileRoute } from "@tanstack/react-router";
import { ReconsiderationWorkspace } from "@/components/workflow/reconsideration-workspace";
import { getWorkflow } from "@/domain/workflows";
export const Route = createFileRoute("/workflows/reconsideration")({head:()=>({meta:[{title:`${getWorkflow("reconsideration").title} — Appeal Mail`},{name:"description",content:getWorkflow("reconsideration").description}, { property: "og:title", content: `${getWorkflow("reconsideration").title} — Appeal Mail` }, { property: "og:description", content: getWorkflow("reconsideration").description }, { name: "twitter:card", content: "summary" }, { name: "twitter:title", content: `${getWorkflow("reconsideration").title} — Appeal Mail` }, { name: "twitter:description", content: getWorkflow("reconsideration").description }], links:[{rel:"canonical",href:"/workflows/reconsideration"}]}),component:ReconsiderationWorkspace});
