import {createFileRoute} from "@tanstack/react-router";import{ScholarshipAppealWorkspace}from"@/components/workflow/scholarship-appeal-workspace";import{getWorkflow}from"@/domain/workflows";export const Route=createFileRoute("/workflows/scholarship-appeal")({head:()=>({meta:[{title:`${getWorkflow("scholarship-appeal").title} — Appeal Mail`},{name:"description",content:getWorkflow("scholarship-appeal").description}]}),component:ScholarshipAppealWorkspace});
