import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { VA_CLAIM_APPEAL_PRICING } from "@/domain/va-claim-appeal-gold";

export const Route = createFileRoute("/workflows/va-claim-appeal")({ head:()=>({meta:[{title:"VA Claim Appeal & Decision Review — Authority-First | Appeal Mail"},{name:"description",content:"Analyze a VA decision, verify the available decision-review lane, map evidence and errors, and prepare a human-reviewed response with transparent packet pricing."},{property:"og:title",content:"VA Claim Appeal & Decision Review"},{property:"og:description",content:"VA decision review analysis with current VA authority, evidence mapping, Gemini drafting, independent validation, pricing, and mailing proof."}],links:[{rel:"canonical",href:"/workflows/va-claim-appeal"}]}),component:VaClaimAppealPage});

function VaClaimAppealPage(){const[file,setFile]=useState<File|null>(null);const[status,setStatus]=useState("");const[result,setResult]=useState<any>(null);async function analyze(){if(!file)return;setStatus("Analyzing the VA decision and checking the available review lane against current VA guidance…");const form=new FormData();form.set("document",file);try{const r=await fetch("/api/workflows/va-claim-appeal/analyze",{method:"POST",body:form});const p=await r.json();if(!r.ok)throw new Error(p.error||"Unable to analyze the decision.");setResult(p);setStatus("Analysis complete. Review the lane, authority, evidence gaps, and unresolved items before drafting.")}catch(e){setStatus(e instanceof Error?e.message:"Unable to analyze the decision.")}}
return <main className="mx-auto max-w-6xl px-6 py-12"><section className="rounded-3xl bg-slate-950 p-8 text-white shadow-xl"><p className="text-sm font-semibold uppercase tracking-[0.22em] text-slate-300">VA Decision Review • Authority-first</p><h1 className="mt-4 max-w-4xl text-4xl font-bold tracking-tight md:text-6xl">Appeal a VA Claim Decision</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-300">Start with your VA decision letter. The workflow separates the decision record from governing review rules, evaluates Supplemental Claim, Higher-Level Review, and Board Appeal paths, maps evidence and error issues, and requires your approval before mailing.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{[["Authority raised","Current VA decision-review guidance and your decision letter are treated as controlling sources. Unsupported procedure stays unresolved."],["Evidence discipline","New evidence, missing support, factual disputes, and review-lane limitations are surfaced without inventing service, medical, rating, or effective-date facts."],["Transparent pricing","Preparation, response sheets, supporting sheets, mailing method, and packet surcharges are shown before checkout."]].map(([a,c])=><div key={a} className="rounded-2xl border border-white/10 bg-white/5 p-5"><h2 className="font-semibold">{a}</h2><p className="mt-2 text-sm leading-6 text-slate-300">{c}</p></div>)}</div></section><section className="mt-10 grid gap-8 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-3xl border border-slate-200 bg-white p-7"><h2 className="text-2xl font-semibold">What we analyze</h2><ol className="mt-6 space-y-4">{["Identify the VA decision, issues, dates, reasons, evidence references, and instructions.","Separate Supplemental Claim, Higher-Level Review, and Board Appeal requirements and evidence limits.","Identify supported factual disputes, potential error issues, evidence gaps, and unresolved authority questions.","Draft and independently validate the response against the decision and current VA sources.","Require explicit human approval before final packet pricing, payment, and mailing."].map((x,i)=><li key={x} className="flex gap-4"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-slate-900 text-sm font-bold text-white">{i+1}</span><span className="leading-7 text-slate-700">{x}</span></li>)}</ol></div><div className="rounded-3xl border border-slate-200 bg-slate-50 p-7"><h2 className="text-2xl font-semibold">Start with your VA decision</h2><p className="mt-3 text-sm leading-6 text-slate-600">Starting price: <strong>${VA_CLAIM_APPEAL_PRICING.preparationFee.toFixed(2)}</strong> plus mailing, assuming {VA_CLAIM_APPEAL_PRICING.includedResponsePages} response pages and no supporting-document sheets.</p><input className="mt-6 block w-full rounded-xl border border-slate-300 bg-white p-3 text-sm" type="file" accept="application/pdf,image/png,image/jpeg" onChange={e=>setFile(e.target.files?.[0]||null)}/><button className="mt-4 w-full rounded-xl bg-slate-900 px-5 py-3 font-semibold text-white disabled:opacity-40" disabled={!file} onClick={analyze}>Analyze VA decision</button>{status&&<p className="mt-4 text-sm text-slate-600">{status}</p>}{result&&<div className="mt-6 rounded-2xl bg-white p-5"><h3 className="font-semibold">Decision snapshot</h3><dl className="mt-3 space-y-2 text-sm"><div><dt className="inline font-medium">Decision date: </dt><dd className="inline">{result.analysis?.decisionDate||"Not identified"}</dd></div><div><dt className="inline font-medium">Review lane: </dt><dd className="inline">{result.analysis?.recommendedReviewLane||"unresolved"}</dd></div><div><dt className="inline font-medium">Deadline status: </dt><dd className="inline">{result.analysis?.deadlineStatus||"unverified"}</dd></div></dl></div>}</div></section><section className="mt-10 rounded-3xl border border-slate-200 bg-white p-7"><h2 className="text-2xl font-semibold">Transparent pricing</h2><div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{[["Preparation",`$${VA_CLAIM_APPEAL_PRICING.preparationFee.toFixed(2)}`],["Included response",`${VA_CLAIM_APPEAL_PRICING.includedResponsePages} pages`],["Extra response",`$${VA_CLAIM_APPEAL_PRICING.responsePagePrice.toFixed(2)}/sheet`],["Supporting evidence",`$${VA_CLAIM_APPEAL_PRICING.supportingPagePrice.toFixed(2)}/sheet`],["Standard mail",`$${VA_CLAIM_APPEAL_PRICING.standardMail.toFixed(2)}`],["Certified",`$${VA_CLAIM_APPEAL_PRICING.certifiedMail.toFixed(2)}`],["Certified + return receipt",`$${VA_CLAIM_APPEAL_PRICING.certifiedReturnReceipt.toFixed(2)}`],["Flat packet surcharge",`$${VA_CLAIM_APPEAL_PRICING.flatEnvelopeFee.toFixed(2)} when required`]].map(([a,b])=><div key={a} className="rounded-2xl bg-slate-50 p-4"><div className="text-sm text-slate-500">{a}</div><div className="mt-1 font-semibold">{b}</div></div>)}</div><p className="mt-6 text-sm leading-7 text-slate-600">The exact price is calculated from the approved physical packet. Supporting-document sheets are billed separately when included.</p></section><p className="mt-10 text-sm text-slate-500">Appeal Mail is not a law firm and does not provide legal advice. VA review options and evidence rules are verified against current authoritative VA sources.</p></main>}
